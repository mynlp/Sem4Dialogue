# toy_en_de.yaml

## Where the samples will be written
save_data: ATIS/run/example
## Where the vocab(s) will be written
src_vocab: ATIS/run/example.vocab.src
tgt_vocab: ATIS/run/example.vocab.tgt
# Prevent overwriting existing files in the folder
overwrite: True

# Corpus opts:
data:
    corpus_1:
        path_src: ATIS/ATIS_processed/train_src.txt
        path_tgt: ATIS/ATIS_processed/train_tgt.txt
    valid:
        path_src: ATIS/ATIS_processed/valid_src.txt
        path_tgt: ATIS/ATIS_processed/valid_tgt.txt